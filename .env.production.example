# =============================================================================
# .env (Produção) - Chatbot de Tributos Nova Trento/SC
# Copie este arquivo para ".env" e preencha os valores necessários.
# =============================================================================

# --- LLM ---
LLM_PROVIDER=groq          # groq | openai | xai
LLM_MODEL=llama-3.3-70b-versatile
GROQ_API_KEY=
OPENAI_API_KEY=
XAI_API_KEY=

# --- APP ---
PORT=5000
ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=INFO

# --- RAG / Chroma ---
CHROMA_DIR=/app/chroma_data
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# --- WAHA ---
WAHA_API_URL=http://waha:3000
WAHA_API_KEY=tributos_nova_trento_2025_api_key_fixed
WAHA_SESSION=default
WAHA_TIMEOUT=10

# --- Fluxo/menus ---
FLOW_JSON_PATH=fluxo_novatrento.json

# --- HTTPS / Proxy (Caddy) ---
DOMAIN=                 # ex.: chatbot.seudominio.com
LETSENCRYPT_EMAIL=      # email para certificados Let's Encrypt

# (Opcional) Basic Auth para /n8n e /waha
# Gere o hash com:
#   - Linux: docker run --rm caddy:2-alpine caddy hash-password --plaintext "SuaSenha"
#   - Windows: ./scripts/gen-caddy-passhash.ps1 -Plaintext "SuaSenha"
N8N_USER=admin
N8N_PASSWORD_HASH=
WAHA_USER=admin
WAHA_PASSWORD_HASH=
